export const selectToken = (state) => state.user.token;

export const selectUser = (state) => state.user;

export const selectUserCardIds = (state) => state.user.userCardIds;

export const selectUserCards = (state) => state.user.userCards;
